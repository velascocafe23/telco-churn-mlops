[project]
name = "template-data-science-container"
version = "0.1.0"
description = "Template for data science and AI Projects with the last stable version of python and devcontainer"
authors = [
    "Jose R. Zapata <joserzapata.github.io>",
]
requires-python = ">=3.12"
readme = "README.md"

dependencies = [

]

[project.urls]
documentation = "https://.github.io/template-data-science-container"
repository = "https://github.com//template-data-science-container"


[dependency-groups]
dev = [
    "pre-commit>=4.6.2", # Code linting and formatting
    "pytest>=9.1.1", # Testing framework
    "pytest-cookies>=0.7.0",
    "pytest-cov>=7.1.0",
    "cruft[pyproject]>=2.16.0",# Automated Cookiecutter template synchronization
]

#

[tool.uv]
package = false
default-groups = [
    "dev",
#
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.coverage.paths]
source = ["src/"]

[tool.coverage.run]
source = ["src"]
omit = ["**/__init__.py"]
